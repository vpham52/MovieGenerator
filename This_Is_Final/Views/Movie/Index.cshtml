@*@model Movie

@{
    ViewData["Title"] = "Movie Details";
}

<style>
    body {
        background-color: #1A2F40;
    }

    .card-header {
        background-color: #F4D03F;
        color: #1A2F40;
        text-align: center;
        font-weight: bold;
        font-size: 2rem;
        border-bottom: 3px solid #1A2F40;
    }

    .movie-icon {
        font-size: 3rem;
        color: #F4D03F;
        margin-right: 10px;
    }

    .btn-random {
        background-color: #F4D03F;
        color: #1A2F40;
        border-radius: 25px;
        padding: 15px;
        font-size: 1.5rem;
        margin-top: 20px;
        box-shadow: 2px 2px 10px #888888;
        transition: all 0.2s ease-in-out;
    }

        .btn-random:hover {
            transform: scale(1.1);
        }
</style>

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card my-5">
                <div class="card-header">
                    <i class="fas fa-film movie-icon"></i> @Model.Title
                </div>
                <div class="card-body">
                    <p><strong>Year:</strong> @Model.Year</p>
                    <p><strong>Genre:</strong> @Model.Genre</p>
                    <p><strong>Rank:</strong> @Model.Rank</p>
                    <p><strong>Rating:</strong> @Model.Rating</p>
                    <p><strong>Description:</strong> @Model.Description</p>

                </div>
                <div class="text-center">
                    <form method="get" action="@Url.Action("Index", "Movie")">
                        <button type="submit" class="btn-random">
                            <i class="fas fa-redo"></i>
                            Generate Another Random Movie!
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://kit.fontawesome.com/33fa818d83.js" crossorigin="anonymous"></script>
*@






@model Movie

@{
    ViewData["Title"] = "Movie Details";
}

<style>
    body {
        background-image: linear-gradient(45deg, #1A2F40, #33425B);
        font-family: Arial, sans-serif;
    }

    .card-header {
        background-color: #F4D03F;
        color: #1A2F40;
        text-align: center;
        font-weight: bold;
        font-size: 2.5rem;
        border-bottom: 3px solid #1A2F40;
        padding: 20px;
    }

    .movie-icon {
        font-size: 3.5rem;
        color: #F4D03F;
        margin-right: 10px;
    }

    .btn-random {
        background-color: #F4D03F;
        color: #1A2F40;
        border-radius: 25px;
        padding: 15px;
        font-size: 1.5rem;
        margin-top: 20px;
        box-shadow: 2px 2px 10px #888888;
        transition: all 0.2s ease-in-out;
    }

        .btn-random:hover {
            transform: scale(1.1);
        }

    .card-body {
        background-color: #F0E68C;
        padding: 30px;
        border-radius: 0 0 15px 15px;
    }

        .card-body p {
            font-size: 1.3rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .card-body strong {
            color: #1A2F40;
        }

    .card {
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2), 0 6px 20px 0 rgba(0, 0, 0, 0.19);
        border-radius: 15px;
        overflow: hidden;
    }
</style>

<div class="container">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card my-5">
                <div class="card-header">
                    <i class="fas fa-film movie-icon"></i> 
                    <a href="@Model.Trailer" style="text-decoration-line:none" target="_blank">@Model.Title</a>
                </div>
                <div class="card-body">
                    <p><center><img src="@Model.Image"></center></p>
                    <p><strong>Year:</strong> @Model.Year</p>
                    <p><strong>Genre:</strong> @Model.Genre</p>
                    <p><strong>Directed by:</strong> @Model.Director</p>
                    <p><strong>Written by:</strong> @Model.Writers</p>
                    <p><strong>Rank:</strong> @Model.Rank</p>
                    <p><strong>Rating:</strong> @Model.Rating</p>
                    <p><strong>Description:</strong> @Model.Description</p>

                </div>
                <div class="text-center">
                    <form method="get" action="@Url.Action("Index", "Movie")">
                        <button type="submit" class="btn-random">
                            <i class="fas fa-redo"></i>
                            Generate Another Random Movie!
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://kit.fontawesome.com/33fa818d83.js" crossorigin="anonymous"></script>
